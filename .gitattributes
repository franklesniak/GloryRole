# Enforce LF line endings for golden baseline JSON files so byte-exact
# equivalence comparisons against in-memory LF-ended JSON succeed on all
# platforms (Windows, macOS, Linux), regardless of the user's core.autocrlf
# setting.
tests/PowerShell/_fixtures/golden/*.json text eol=lf
